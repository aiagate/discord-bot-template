services:

  db:
    image: postgres
    ports:
      - "5432:5432"
    restart: always
    shm_size: 128mb
    volumes:
      - db_vol:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: example

volumes:
  db_vol:
